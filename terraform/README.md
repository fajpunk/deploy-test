# terraform

This is the Terraform config that was used to create this very repo!
If you want to create such a repo yourself, you can do something like this from this `terraform` directory, assuming you have the `terraform` executable on your path:

```console
$ terraform init
$ TF_VAR_owner=some-org terraform apply
```
